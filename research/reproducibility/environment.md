# Environment Reproducibility

Every reported result must identify the environment used to produce it.

Required fields:

- operating system and version
- shell or job runner
- Python version
- virtual environment or container identifier
- package installation command
- relevant environment variables
- code commit
- config commit or config hash
- dataset version
- model version
- exact command or script

Do not publish or summarize experimental results unless these fields are recoverable.
